# Step 2.3.1 Validation Report

Agent E validation-side hardening is implemented in `soma_retargeter/robotics/v3/validation.py`.

## Implemented

- Immutable Assets44 baseline loader reads:
  `git show 5ad5a001c445c525d4c8bbaf6339dec5c5c2c719:artifacts/retargeting_v3_step2_assets44/summary.json`
- Baseline counts are enforced exactly: 21 `passed`, 3 `partial_passed`, 9 `negative_control_passed`, 11 `algorithm_failed`.
- Full 44 before/after rows no longer infer baseline status from current status.
- Legal transitions are restricted to:
  `passed->passed`, `partial_passed->partial_passed`, `negative_control_passed->negative_control_passed`,
  `algorithm_failed->passed`, and `algorithm_failed->capability_limited_passed`.
- Hardened final count constraints are enforced for the full baseline set.
- Deterministic rerun comparison now covers status, profile hash, rank summary, projection residuals, q vectors,
  task vectors, certificate class/digest, semantic evidence, and task certificate summary.
- Matched `algorithm_failed` profile reruns count as deterministic matches; unexpected rerun failures still fail.

## Tests Run

```bash
PYTHONPATH=. pytest -q tests/v3/test_full_44_hardened_validation_baseline.py tests/v3/test_hardened_deterministic_rerun_profile_fields.py tests/v3/test_hardened_cross_format_negative_control.py
PYTHONPATH=. pytest -q tests/v3/test_full_44_hardened_validation_baseline.py tests/v3/test_hardened_deterministic_rerun_profile_fields.py tests/v3/test_hardened_cross_format_negative_control.py tests/v3/test_deterministic_rerun_validation.py
PYTHONPATH=. python -m py_compile soma_retargeter/robotics/v3/validation.py soma_retargeter/tools/validate_kinematic_profile_v3.py tests/v3/test_full_44_hardened_validation_baseline.py tests/v3/test_hardened_deterministic_rerun_profile_fields.py tests/v3/test_hardened_cross_format_negative_control.py
```

Latest targeted result: 16 passed.

## Integration Notes

Final capability artifacts were not regenerated by Agent E. The next integration run must execute the full 44 validation with
`--deterministic-rerun` after upstream status/certificate fixes are present. If any baseline `passed`, `partial_passed`, or
`negative_control_passed` model changes status, validation will now fail instead of producing a fabricated transition.
