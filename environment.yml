name: soma-retargeter-v2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - git-lfs
  - pip:
      - --extra-index-url https://pypi.nvidia.com
      - -e .
      - pytest==9.1.1
