# SOMA Retargeter 中文入门指南

目标：让一个新机器人 `new_robot` 最快跑通下面两条命令：

```bash
python ./app/bvh_to_csv_converter.py --viewer gl --robot new_robot
python ./app/bvh_to_csv_converter.py --viewer null --robot new_robot
```

下面默认你已经在仓库根目录：

```bash
cd soma-retargeter-autoconfig
```

如果用 `uv` 环境，把下面所有 `python` 换成 `uv run`。

## 1. 先跑通已有机器人

先确认环境没问题：

```bash
python ./app/bvh_to_csv_converter.py --viewer gl --robot roboparty_rpo
```

如果窗口能打开，说明 Python 环境、Newton、Warp、OpenGL 基本可用。

再测 headless：

```bash
python ./app/bvh_to_csv_converter.py --viewer null --robot roboparty_rpo
```

## 2. 准备 new_robot 文件

新机器人最少准备 2 个文件：

1. 机器人 MJCF/XML 文件，例如：

```text
assets/robots/new_robot/mjcf/world.xml
```

2. 最小 retargeter config，例如：

```text
soma_retargeter/configs/new_robot/soma_to_new_robot_retargeter_config.json
```

如果要用 config 优化窗口，额外准备 1 个机器人 T-pose JSON：

```text
soma_retargeter/configs/new_robot/pose_pairs/new_robot_t_pose.json
```

URDF 是可选参考文件；有就注册，没有也可以先不管。

## 3. 写最小 retargeter config

`soma_to_new_robot_retargeter_config.json` 只写人体部位到机器人 link 的对应关系，不要写权重、scaler、初始化帧数、IK 迭代次数。

模板：

```json
{
    "ik_map": {
        "Hips": "base_link",
        "Chest": "torso_link",
        "LeftArm": "left_shoulder_roll_link",
        "LeftForeArm": "left_elbow_link",
        "LeftHand": "left_wrist_link",
        "RightArm": "right_shoulder_roll_link",
        "RightForeArm": "right_elbow_link",
        "RightHand": "right_wrist_link",
        "LeftLeg": "left_hip_roll_link",
        "LeftShin": "left_knee_link",
        "LeftFoot": "left_ankle_roll_link",
        "RightLeg": "right_hip_roll_link",
        "RightShin": "right_knee_link",
        "RightFoot": "right_ankle_roll_link"
    }
}
```

左边的名字必须用上面这些 SOMA 人体部位名；右边改成 `new_robot` 真实存在的 link 名。

## 4. 在 params.py 注册 new_robot

打开根目录的 `params.py`，只需要填四个字典。

示例：

```python
ROBOT_XML_DICT["new_robot"] = ROBOT_ROOT / "new_robot" / "mjcf" / "world.xml"

ROBOT_URDF_DICT["new_robot"] = ROBOT_ROOT / "new_robot" / "urdf" / "new_robot.urdf"

RETARGETER_CONFIG_DICT["new_robot"] = (
    CONFIG_ROOT / "new_robot" / "soma_to_new_robot_retargeter_config.json"
)

POSE_PAIR_JSON_DICT["new_robot"] = {
    "t_pose": CONFIG_ROOT / "new_robot" / "pose_pairs" / "new_robot_t_pose.json",
}
```

如果暂时没有 URDF，可以不写 `ROBOT_URDF_DICT["new_robot"]`。

如果暂时没有 T-pose JSON，可以先写：

```python
POSE_PAIR_JSON_DICT["new_robot"] = {}
```

这种情况下可以先打开重定向窗口，但优化 config 时没有可训练姿态组。

## 5. 可选：准备机器人 T-pose JSON

如果想让系统学习更合适的 scaler config，准备：

```text
soma_retargeter/configs/new_robot/pose_pairs/new_robot_t_pose.json
```

最小模板：

```json
{
    "pose_name": "new_robot_t_pose",
    "description": "Robot T-pose. Use radians.",
    "units": "radians",
    "joint_positions_rad": {
        "left_shoulder_roll_joint": -1.57,
        "right_shoulder_roll_joint": 1.57,
        "left_elbow_joint": 0.0,
        "right_elbow_joint": 0.0
    }
}
```

`joint_positions_rad` 里的 key 必须是机器人模型里的真实关节名，value 用弧度。没有写到的关节会保持模型默认值。

标准 human T-pose 不用准备，代码已经固定从这里读取：

```text
soma_retargeter/assets/standard_human_pos/human_t_pose.json
```

## 6. 打开 config 优化窗口

运行：

```bash
python ./app/pose_optimizer_ui.py --viewer gl --robot new_robot
```

看右侧“注册状态”：

- `MJCF/XML: 已准备`：机器人 XML 路径对了。
- `Retargeter link map: 已准备`：最小 retargeter config 路径对了。
- `Scaler config: 已准备 - 自动生成`：自动生成成功。
- `BVH converter config: 已准备 - 自动生成`：自动生成成功。
- `机器人 pose pair: 已准备 1/5 组`：至少 T-pose 已经准备好。

如果 T-pose 已准备好，点击“开始优化配置”。优化完成后，会自动更新：

```text
soma_retargeter/configs/new_robot/soma_to_new_robot_scaler_config.json
```

这个 scaler config 后面重定向会自动使用，不需要手动改路径。

## 7. 运行可视化重定向

运行：

```bash
python ./app/bvh_to_csv_converter.py --viewer gl --robot new_robot
```

第一次运行会自动生成：

```text
soma_retargeter/configs/new_robot/new_robot_bvh_to_csv_converter_config.json
```

默认会从这里找 BVH：

```text
assets/motions/bvh
```

默认会导出到：

```text
assets/motions/new_robot-export
```

窗口打开后，先用默认 BVH 测一条动作。能看到 human 和 robot 同时动，就说明注册链路已经跑通。

## 8. 运行 headless 批量重定向

确认 viewer 模式成功后，运行：

```bash
python ./app/bvh_to_csv_converter.py --viewer null --robot new_robot
```

它会读取自动生成的 converter config，把 `import_folder` 里的 `.bvh` 批量转成 CSV，写到 `export_folder`。

## 9. 常见问题

`Unknown robot` 或找不到 `new_robot`：
检查 `params.py` 四个字典里是否都用了同一个名字 `new_robot`。

窗口提示 XML 缺少：
检查 `ROBOT_XML_DICT["new_robot"]` 指向的文件是否真实存在。

报 link 找不到：
检查 retargeter config 右侧 link 名是否和 MJCF/XML 里的 body/link 名一致。

报 retargeter `ik_map` 格式错误：
新格式必须是 `"LeftForeArm": "left_elbow_link"`，不要再写旧格式的 `t_body`、`r_body`、`t_weight`、`r_weight`。

优化窗口没有可训练姿态组：
检查 `POSE_PAIR_JSON_DICT["new_robot"]` 是否注册了 `"t_pose"`，以及对应 JSON 是否存在。

重定向动作能跑但姿态不好：
先补准 T-pose；如果还不够，再追加更多机器人姿态 JSON，例如 `natural_down`、`both_arms_forward`。

## 10. 已有机器人命令

RoboParty RPO config 优化：

```bash
python ./app/pose_optimizer_ui.py --viewer gl --robot roboparty_rpo
```

RoboParty RPO 可视化重定向：

```bash
python ./app/bvh_to_csv_converter.py --viewer gl --robot roboparty_rpo
```

RoboParty RPO headless 批量重定向：

```bash
python ./app/bvh_to_csv_converter.py --viewer null --robot roboparty_rpo
```
